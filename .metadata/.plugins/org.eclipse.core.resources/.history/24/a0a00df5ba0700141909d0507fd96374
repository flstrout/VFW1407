// Fred L. Strout
// Project 1 - Functions
// Created On: 7/9/2014

var states = ["Alabama", "California", "Florida", "Louisiana", "Maine", "New Mexico"],
	i=0,
	j=states.length,
	data = "Test Data",
	statesLabel = null;
	
var nextData = function(){
	if(i === j){
		i = 0
		data=states[i]
	}
	else{
		i++
		data=states[i]
	};
	statesLabel = Ti.UI.createLabel({
		text: data,
		backgroundColor: "#fff",
		color: "#000",
		width: 200,
		heigth: 28,
		font: {fontSize: 22, fontFamily: "Arial", fontWeight: "bold"},
		align: "center"
	});
	infoDisplay.add(statesLabel);
};


var previousData = function(){
	if(i === 0){
		i = j
		data=states[i]
	}
	else{
		i--
		data=states[i]
	};
	var statesLabel = Ti.UI.createLabel({
		text: data,
		backgroundColor: "#fff",
		color: "#000",
		width: 200,
		heigth: 28,
		font: {fontSize: 22, fontFamily: "Arial", fontWeight: "bold"},
		align: "center"
	});
	infoDisplay.add(statesLabel);
};

buttonNext.addEventListener("click", nextData);
buttonPrevious.addEventListener("click", previousData);

